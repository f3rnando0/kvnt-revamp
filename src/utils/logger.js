export const ok = (parameter) => {
    console.log(`[+] ${parameter}`)
};
export const warn = (parameter) => {
    console.log(`[*] ${parameter}`)
};
export const error = (parameter) => {
    console.log(`[-] ${parameter}`)
};
